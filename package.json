{
  "name": "katallaxie.dev",
  "private": true,
  "scripts": {
    "start": "hugo server --buildDrafts --cleanDestinationDir --logLevel debug --disableFastRender --gc --minify --noHTTPCache --printI18nWarnings --templateMetrics --templateMetricsHints"
  },
  "browserslist": [
    "last 1 version",
    "> 1%",
    "maintained node versions",
    "not dead"
  ],
  "dependencies": {
    "@fullhuman/postcss-purgecss": "^5.0.0",
    "@tailwindcss/aspect-ratio": "^0.x",
    "@tailwindcss/forms": "^0.x",
    "@tailwindcss/line-clamp": "^0.x",
    "@tailwindcss/typography": "^0.5.9",
    "autoprefixer": "^10.x",
    "postcss": "^8.x",
    "postcss-cli": "^9.x",
    "tailwindcss": "^3.x",
    "tailwindcss-writing-mode": "^1.0.0"
  }
}
